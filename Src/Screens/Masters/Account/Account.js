import React from "react";
import {
    View,
    SafeAreaView,
    StyleSheet
} from "react-native";

const Account = ({ navigation }) => {
    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({

})

export default Account